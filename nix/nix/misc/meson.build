# nix misc
#============================================================================

nix_inc += include_directories('.')

nix_misc_dirs = [
    'get-env',
]

foreach dir : nix_misc_dirs
    subdir(dir)
endforeach
